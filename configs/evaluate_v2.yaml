data:
  pgn: data/games.pgn
  batch_size: 64
  winners_only: false

decoder:
  d_model: 256
  n_heads: 8
  n_layers: 4
  dim_feedforward: 1024
  dropout: 0.1
  max_seq_len: 512
  move_vocab_size: 1971

eval:
  checkpoint: checkpoints/chess_v2_50games.pt
  top_n: 5
