# Evaluation configuration
#
# Usage:
#   python -m scripts.evaluate \
#       --config configs/evaluate.yaml \
#       --checkpoint checkpoints/lichess_50k_v1.pt
#
# The --checkpoint CLI arg overrides eval.checkpoint below.

data:
  pgn: "data/games.pgn"
  winners_only: false

eval:
  checkpoint: ""     # override with --checkpoint at CLI
  game_index: 0
  top_n: 5
